<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:elevation="0dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="72dp"
            android:paddingHorizontal="20dp"
            android:paddingVertical="8dp">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:layout_toStartOf="@+id/llHeaderActions"
                android:layout_marginEnd="12dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvGreeting"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Halo, Admin Safety"
                    android:textColor="@color/text_low"
                    android:textSize="12sp"
                    android:fontFamily="sans-serif-medium"
                    android:letterSpacing="0.02" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="SCANNING CONSTRUCTION"
                    android:textColor="@color/primary_dark"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:letterSpacing="-0.01" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/llHeaderActions"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:orientation="horizontal">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnManageAccounts"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Akun"
                    android:textColor="#3182CE"
                    android:minWidth="0dp"
                    android:paddingHorizontal="8dp"
                    android:textSize="12sp"
                    android:fontFamily="sans-serif-medium"
                    app:icon="@drawable/ic_user_outline"
                    app:iconTint="#3182CE"
                    app:iconPadding="2dp"
                    app:iconSize="24dp"
                    app:iconGravity="top" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnLogout"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:text="Keluar"
                    android:textColor="#E53E3E"
                    android:minWidth="0dp"
                    android:paddingHorizontal="8dp"
                    android:textSize="12sp"
                    android:fontFamily="sans-serif-medium"
                    app:icon="@drawable/ic_logout"
                    app:iconTint="#E53E3E"
                    app:iconPadding="2dp"
                    app:iconSize="24dp"
                    app:iconGravity="top" />
            </LinearLayout>

        </RelativeLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- Floating Bottom Navigation -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/bottomNavCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="32dp"
        app:cardCornerRadius="36dp"
        app:cardBackgroundColor="@color/bg_surface"
        app:cardElevation="12dp"
        app:strokeWidth="1dp"
        app:strokeColor="@color/divider_light">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:padding="6dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnNavScan"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="wrap_content"
                android:layout_height="52dp"
                android:text="Scan"
                android:textColor="@color/text_high"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium"
                android:paddingHorizontal="24dp"
                app:cornerRadius="26dp"
                app:backgroundTint="@android:color/transparent"
                app:icon="@drawable/ic_nav_scan"
                app:iconGravity="textStart"
                app:iconTint="@color/text_high"
                app:iconSize="20dp"
                app:iconPadding="8dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                app:elevation="0dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnNavDirectory"
                android:layout_width="wrap_content"
                android:layout_height="52dp"
                android:layout_marginStart="4dp"
                android:text="Directory"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium"
                android:paddingHorizontal="24dp"
                app:cornerRadius="26dp"
                app:backgroundTint="@color/accent_teal"
                app:icon="@drawable/ic_nav_directory"
                app:iconGravity="textStart"
                app:iconTint="@color/white"
                app:iconSize="20dp"
                app:iconPadding="8dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                app:elevation="0dp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
